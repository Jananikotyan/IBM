[build]
builder = "NIXPACKS"
rootDirectory = "sehat-saathi"

[deploy]
startCommand = "uvicorn app.api.routes:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
