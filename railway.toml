[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python scripts/ingest_documents.py && uvicorn app.api.routes:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
